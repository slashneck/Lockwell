<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        The setup program is the first thing anyone sees of Lockwell, so it uses the same
        palette as the app rather than a look of its own: the deep blue-black surfaces and
        the teal-to-cyan accent from Lockwell/Themes/Theme.xaml.

        One rule carried over from the app, and it is not a style preference: no Effect on
        an element that contains text. WPF renders a blurred or shadowed container by
        rasterising its whole subtree, which softens the glyphs inside it. That bug was
        found and fixed three times in the app before a check was written for it. Shadows
        here are drawn as border strokes and gradients instead.
    -->

    <Color x:Key="InstallerBg0Color">#06080D</Color>
    <Color x:Key="InstallerBg1Color">#0A0E16</Color>
    <Color x:Key="InstallerBg2Color">#11151F</Color>
    <Color x:Key="InstallerBg3Color">#1A2030</Color>
    <Color x:Key="InstallerStrokeColor">#22293A</Color>
    <Color x:Key="InstallerAccentColor">#2DD4BF</Color>
    <Color x:Key="InstallerAccent2Color">#38BDF8</Color>
    <Color x:Key="InstallerVioletColor">#8B5CF6</Color>
    <Color x:Key="InstallerTextColor">#EAF1F8</Color>
    <Color x:Key="InstallerMutedColor">#8694A8</Color>
    <Color x:Key="InstallerFaintColor">#5C6A7E</Color>

    <SolidColorBrush x:Key="InstallerBg0" Color="{StaticResource InstallerBg0Color}" />
    <SolidColorBrush x:Key="InstallerBg1" Color="{StaticResource InstallerBg1Color}" />
    <SolidColorBrush x:Key="InstallerBg2" Color="{StaticResource InstallerBg2Color}" />
    <SolidColorBrush x:Key="InstallerBg3" Color="{StaticResource InstallerBg3Color}" />
    <SolidColorBrush x:Key="InstallerStroke" Color="{StaticResource InstallerStrokeColor}" />
    <SolidColorBrush x:Key="InstallerAccent" Color="{StaticResource InstallerAccentColor}" />
    <SolidColorBrush x:Key="InstallerAccent2" Color="{StaticResource InstallerAccent2Color}" />
    <SolidColorBrush x:Key="InstallerViolet" Color="{StaticResource InstallerVioletColor}" />
    <SolidColorBrush x:Key="InstallerText" Color="{StaticResource InstallerTextColor}" />
    <SolidColorBrush x:Key="InstallerMuted" Color="{StaticResource InstallerMutedColor}" />
    <SolidColorBrush x:Key="InstallerFaint" Color="{StaticResource InstallerFaintColor}" />
    <SolidColorBrush x:Key="InstallerWarning" Color="#F59E0B" />
    <SolidColorBrush x:Key="InstallerDanger" Color="#FB7185" />
    <SolidColorBrush x:Key="InstallerAccentSoft" Color="#242DD4BF" />

    <LinearGradientBrush x:Key="InstallerShellGradient" StartPoint="0,0" EndPoint="0.6,1">
        <GradientStop Color="#0B101A" Offset="0" />
        <GradientStop Color="#070A11" Offset="0.55" />
        <GradientStop Color="#05070C" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="InstallerRailGradient" StartPoint="0,0" EndPoint="0.3,1">
        <GradientStop Color="#101725" Offset="0" />
        <GradientStop Color="#0A0F1A" Offset="0.6" />
        <GradientStop Color="#080C14" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="InstallerCardGradient" StartPoint="0,0" EndPoint="0.7,1">
        <GradientStop Color="#14FFFFFF" Offset="0" />
        <GradientStop Color="#06FFFFFF" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="InstallerCardStroke" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#2AFFFFFF" Offset="0" />
        <GradientStop Color="#0CFFFFFF" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="InstallerAccentBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#2DD4BF" Offset="0" />
        <GradientStop Color="#22D3EE" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="InstallerAccentBrushHover" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#45E2CE" Offset="0" />
        <GradientStop Color="#43DDF5" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="InstallerMarkBrush" StartPoint="0,0" EndPoint="1,0.4">
        <GradientStop Color="#5EEAD4" Offset="0" />
        <GradientStop Color="#7DD3FC" Offset="0.5" />
        <GradientStop Color="#A78BFA" Offset="1" />
    </LinearGradientBrush>

    <!-- Typography -->

    <Style x:Key="InstallerWordmark" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource InstallerText}" />
        <Setter Property="FontSize" Value="19" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="InstallerTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource InstallerText}" />
        <Setter Property="FontSize" Value="21" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="InstallerSubtitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource InstallerMuted}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="19" />
    </Style>

    <Style x:Key="InstallerCaption" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource InstallerFaint}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="16" />
    </Style>

    <Style x:Key="InstallerEyebrow" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource InstallerAccent}" />
        <Setter Property="FontSize" Value="10.5" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="InstallerSectionLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource InstallerText}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,8" />
    </Style>

    <!-- Buttons -->

    <Style x:Key="InstallerPrimaryButton" TargetType="Button">
        <Setter Property="Foreground" Value="#052A26" />
        <Setter Property="Background" Value="{StaticResource InstallerAccentBrush}" />
        <Setter Property="BorderBrush" Value="#5EEAD4" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="MinWidth" Value="86" />
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.4" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource InstallerAccentBrushHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallerGhostButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource InstallerText}" />
        <Setter Property="Background" Value="#12FFFFFF" />
        <Setter Property="BorderBrush" Value="{StaticResource InstallerStroke}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="MinWidth" Value="86" />
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.4" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#1FFFFFFF" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#3A465C" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallerOptionButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource InstallerText}" />
        <Setter Property="Background" Value="{StaticResource InstallerCardGradient}" />
        <Setter Property="BorderBrush" Value="{StaticResource InstallerCardStroke}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="16,14" />
        <Setter Property="Margin" Value="0,0,0,10" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Rule"
                                    Grid.Column="0"
                                    Width="3"
                                    CornerRadius="2"
                                    Margin="0,2,14,2"
                                    Background="{StaticResource InstallerStroke}" />
                            <ContentPresenter Grid.Column="1"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource InstallerAccent}" />
                            <Setter TargetName="Rule" Property="Background" Value="{StaticResource InstallerAccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#0AFFFFFF" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.4" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallerChromeCloseButton" TargetType="Button">
        <Setter Property="Width" Value="34" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Foreground" Value="{StaticResource InstallerMuted}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="7">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FB7185" />
                            <Setter Property="Foreground" Value="#0A0E16" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Inputs -->

    <Style x:Key="InstallerTextBox" TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource InstallerText}" />
        <Setter Property="Background" Value="#0A0E16" />
        <Setter Property="BorderBrush" Value="{StaticResource InstallerStroke}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="11,9" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="CaretBrush" Value="{StaticResource InstallerAccent}" />
        <Setter Property="SelectionBrush" Value="{StaticResource InstallerAccent}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource InstallerAccent}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#3A465C" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallerCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource InstallerText}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="Margin" Value="0,0,0,12" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="Box"
                                Width="18"
                                Height="18"
                                CornerRadius="5"
                                VerticalAlignment="Center"
                                Background="#0A0E16"
                                BorderBrush="{StaticResource InstallerStroke}"
                                BorderThickness="1">
                            <Path x:Name="Tick"
                                  Data="M 3.5 8.5 L 7 12 L 14.5 4.5"
                                  Stroke="#052A26"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{StaticResource InstallerAccentBrush}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="#5EEAD4" />
                            <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource InstallerAccent}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Surfaces -->

    <Style x:Key="InstallerCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource InstallerCardGradient}" />
        <Setter Property="BorderBrush" Value="{StaticResource InstallerCardStroke}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="14,12" />
    </Style>

    <Style x:Key="InstallerReviewRow" TargetType="Border">
        <Setter Property="Background" Value="#0CFFFFFF" />
        <Setter Property="BorderBrush" Value="#18FFFFFF" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="9" />
        <Setter Property="Padding" Value="13,11" />
        <Setter Property="Margin" Value="0,0,0,8" />
    </Style>

    <Style x:Key="InstallerProgressBar" TargetType="ProgressBar">
        <Setter Property="Height" Value="6" />
        <Setter Property="Foreground" Value="{StaticResource InstallerAccentBrush}" />
        <Setter Property="Background" Value="#14FFFFFF" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border Background="{TemplateBinding Background}" CornerRadius="3" ClipToBounds="True">
                        <Border x:Name="PART_Track">
                            <Border x:Name="PART_Indicator"
                                    HorizontalAlignment="Left"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="3" />
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Step rail -->

    <Style x:Key="InstallerStepNumber" TargetType="Border">
        <Setter Property="Width" Value="22" />
        <Setter Property="Height" Value="22" />
        <Setter Property="CornerRadius" Value="11" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- Scrolling -->

    <Style x:Key="InstallerScrollRepeatButton" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Opacity" Value="0" />
        <Setter Property="Width" Value="0" />
        <Setter Property="Height" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallerScrollBar" TargetType="ScrollBar">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Width" Value="4" />
        <Setter Property="MinWidth" Value="4" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid x:Name="Bg" Background="Transparent">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.LineUpCommand"
                                              Style="{StaticResource InstallerScrollRepeatButton}" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border x:Name="ThumbBd"
                                                    Background="#3A465C"
                                                    CornerRadius="2"
                                                    Margin="0,1" />
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ThumbBd" Property="Background" Value="{StaticResource InstallerAccent}" />
                                                </Trigger>
                                                <Trigger Property="IsDragging" Value="True">
                                                    <Setter TargetName="ThumbBd" Property="Background" Value="{StaticResource InstallerAccent}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.LineDownCommand"
                                              Style="{StaticResource InstallerScrollRepeatButton}" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallerScrollViewer" TargetType="ScrollViewer">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ScrollContentPresenter Grid.Column="0"
                                                Margin="{TemplateBinding Padding}"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}" />
                        <ScrollBar Grid.Column="1"
                                   Style="{StaticResource InstallerScrollBar}"
                                   Orientation="Vertical"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Value="{TemplateBinding VerticalOffset}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
